# 测试与验证方案

基线版本：0.3.0

## 1. 验证层次

1. 静态数据验证：配置结构、数量、ID唯一性和时间顺序；
2. 逻辑验证：总量守恒、设备状态机、路径—标识一致性；
3. 界面验证：关键事件可达、三层同图、底图可平移缩放、设备可查询、路线可见、监控窗可拖拽折叠；
4. 性能验证：帧率、刷新、跳转、内存和48小时稳定性；
5. 专家验证：流程符合应急脚本、组织策略合理、输出可解释。

## 2. 当前自动化验证

执行 `npm test`，检查：

- 初始区域人数之和等于37,000；
- 15个事件严格递增，覆盖09:00—09:47；
- 五类设备数量为4/6/5/4/20；
- 6个监控点均明确标识为未接入真实视频的仿真占位源；
- 总体布局底图被集成且界面不存在楼层标签切换；
- 总图视口隐藏滚动条，具备拖拽、缩放、复位和监控面板折叠控制；
- 8条路径齐全；
- 数据库包含快照、指标、设备日志、一致性检查、需求追溯和变更记录；
- 版本号和核心文档同步；
- 生产构建成功。

## 3. 关键验收用例

| 用例 | 操作 | 预期 |
|---|---|---|
| TC-01 | 跳转09:05 | 总人数37,000，西南/西北闸机关闭，南北闸机限行 |
| TC-02 | 跳转09:17 | 32B广播进入自动播报/应急宣传状态 |
| TC-03 | 跳转09:21 | 疏散路径1激活，西广场通道单向开启 |
| TC-04 | 跳转09:26 | 第八售票处显示退票公告 |
| TC-05 | 跳转09:35 | 绿色通道开启，路径3/4反向回流 |
| TC-06 | 跳转09:46 | 候车厅约20,000，总人数仍为37,000 |
| TC-07 | 拖动到任意时刻 | 时刻、事件、区域和设备状态同步更新 |
| TC-08 | 点击设备 | 弹窗显示ID、当前状态和联动依据 |
| TC-09 | 跳转09:21、09:29、09:35 | 对应疏散、高普联动和回流路线动态显示且箭头方向正确 |
| TC-10 | 拖动任一监控窗标题栏 | 窗口随指针移动，画面持续显示仿真源标识 |
| TC-11 | 拖动总图并点击放大/缩小/复位 | 底图平移缩放正常，视口不出现滚动条 |
| TC-12 | 点击单窗折叠与整组收起 | 单个画面和监控面板按当前状态展开或收起 |

## 4. 生产版性能试验

标准配置需记录：平均/1%低帧率、峰值内存、CPU/GPU占用、热力端到端延迟、时间跳转P95、设备刷新P95、数据库写入P95、丢帧/缺快照率。测试规模至少包括10k、20k、40k个体或等价LOD，并执行48小时稳定性试验。

## 5. 实验效能指标

- 疏散完成时间；
- 峰值密度及持续时间；
- 超阈值区域数量和累计暴露人分钟；
- 平均/95分位通行时间；
- 队列峰值与清空时间；
- 路径切换次数与绕行代价；
- 设备响应延迟和规则冲突数；
- 方案相对基线的改善率及置信区间。

## 6. 验收证据

每次正式验收保存代码版本、配置版本、随机种子、运行参数、环境、原始日志、快照、测试报告、缺陷清单和签字记录。任何仅凭截图而无运行批次与数据校验和的结果，不作为论文或专利证据。
